# Binarios y artefactos de build
/dist/
/dots
/ds
# Binario temporal que genera el arnés de integración
/tests/integration/dots
# Binario del motor que compila el workflow Lab (el contenido de muestra sí se
# versiona, en tests/lab/content)
/tests/lab/dots

# Devbox: el store realizado es local de cada máquina (el manifiesto y el lock
# sí se versionan para tener un entorno reproducible).
/.devbox/

# Capa agéntica: NADA de cerebro dentro del repo (vive en el .pws umbrella).
/.claude/
/CLAUDE.md

# Basura de editores / SO
*.swp
*~
.DS_Store
